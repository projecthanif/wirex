// Assuming you have some code to toggle the popup
var popup = document.querySelector('.popup');
// Toggle function, you might have something similar already
function togglePopup() {
  popup.classList.toggle('active');
}
